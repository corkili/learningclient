<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.corkili.learningclient">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ui.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.RegisterActivity"
            android:label="新用户注册" />
        <activity
            android:name=".ui.activity.LoginActivity"
            android:label="用户登录" />
        <activity
            android:name=".ui.activity.TeacherMainActivity"
            android:theme="@style/AppTheme.NoTitle" />
        <activity
            android:name=".ui.activity.TeacherCourseEditActivity"
            android:label="课程信息编辑" />
        <activity
            android:name=".ui.activity.TeacherCourseManageActivity"
            android:label="课程详情" />
        <activity
            android:name=".ui.activity.CourseCommentActivity"
            android:label="课程评论" />
        <activity
            android:name=".ui.activity.TeacherCourseSubscriptionActivity"
            android:label="课程订阅列表" />
        <activity
            android:name=".ui.activity.ForumActivity"
            android:label="课程讨论区" />

    </application>

</manifest>